Git exportdir - Export one or more directories from a git repo.

    -i, --interactive                Interactive Mode
        --debug                      Debug Mode
    -d, --directory                  Directory to copy
    -r, --repo [Git Repo]            Repository to clone
    -h, --help                       Displays this help message
        --version                    Show version
